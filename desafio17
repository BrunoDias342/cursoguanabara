# faça um programa que leia o catedo oposto e do catedo adjacente de um triangulo retangulo
# calcule e mostre o cumprimento da hipotenusa

import math

o = float(input(" Diga o tamanho do catedo oposto "))
a = float(input(" Diga o tamanho do catedo adjacente  "))
c = (math.pow(o,2) + math.pow(a,2))
r = math.sqrt(c)
print(f"O valor da hipotenusa é {r:.2f} ")
